name: Deploy Support Chat
on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Inject API Key
        run: |
          # Geht in den Ordner support und ersetzt dort den Key
          sed -i 's/__GROQ_API_KEY__/${{ secrets.GROQ_API_KEY }}/g' support/index.html

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: .
          branch: gh-pages
